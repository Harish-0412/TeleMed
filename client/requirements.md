## Packages
date-fns | formatting dates for messages and consultations
lucide-react | icons for UI elements
clsx | conditional class names
tailwind-merge | class merging utility

## Notes
Authentication is handled via Replit Auth (useAuth hook).
Profiles need to be checked on login.
The app distinguishes between 'doctor' and 'facilitator' roles.
